# Gradle
.gradle/
/build
/*/build
/*/*/build

# Local configuration — machine-specific, never shared (contains sdk.dir)
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Generated Room schema history — real files here, once generated, should be
# committed (they're the migration-testing safety net), just not build output.
!/app/schemas/

# Keystores — never commit signing material
*.jks
*.keystore
